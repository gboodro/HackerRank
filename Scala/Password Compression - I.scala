// https://www.hackerrank.com/contests/lambda-calculi-10/challenges/password-compression-i

object Solution {

    def main(args: Array[String]) {
        println(readLine.split(" ").map(_.charAt(0)).mkString(""));
    }
}